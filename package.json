{
  "name": "heroku-nextjs-custom-server-express",
  "version": "1.0.0",
  "description": "Deploy Next.js server-side React apps using a custom Express server to Heroku",
  "scripts": {
    "dev": "node server.js -p $PORT",
    "build": "next build",
    "heroku-postbuild": "heroku-nextjs-build",
    "start": "node server.js -p $PORT",
    "test": "jest"
  },
  "dependencies": {
    "@mars/heroku-nextjs-build": "^2.0.0-beta.1",
    "express": "^4.15.2",
    "next": "^2.0.0-beta.36",
    "react": "^15.4.2",
    "react-dom": "^15.4.2"
  },
  "engines": {
    "node": "6.9.x"
  },
  "cacheDirectories": [
    "nextjs/node_modules"
  ],
  "devDependencies": {
    "jest": "^19.0.2"
  }
}
